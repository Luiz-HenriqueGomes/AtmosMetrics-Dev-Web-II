# ============================================================
# AtmosMetrics — .env.example
# ============================================================
# INSTRUÇÕES:
# 1. Copie este arquivo: cp .env.example .env  (Linux/Mac)
#                        copy .env.example .env (Windows)
# 2. Preencha os valores abaixo no arquivo .env criado
# 3. NUNCA envie o arquivo .env para o GitHub!
# ============================================================

# --- PostgreSQL ---
POSTGRES_DB=atmosmetrics
POSTGRES_USER=atmos_user
POSTGRES_PASSWORD=SuaSenhaSeguraAqui
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
